# SwcPage Placeholder

(fill in once the SWC screen is designed — reuse the report/complaints breakdown from the architecture blueprint: capture → location → review-submit steps, plus a complaints list/detail)
